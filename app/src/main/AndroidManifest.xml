<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sdp13epfl2021.projmag">

    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Projmag">
        <activity android:name=".activities.TagsSelectorActivity"></activity>
        <activity android:name=".activities.ProfilePageActivity" />
        <activity
            android:name=".video.VideoSubtitlingActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.CVCreationActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.ProjectInformationActivity"
            android:parentActivityName=".activities.ProjectsListActivity" />
        <activity android:name=".activities.SignInActivity" />
        <activity
            android:name=".Form"
            android:screenOrientation="portrait" />
        <activity android:name=".activities.ProjectsListActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>